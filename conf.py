# This file needs to be kept in the parent directory, otherwise the BASE_DIR variable will be problematic
from pathlib import Path

BASE_DIR = Path(__file__).parent.resolve()
XHS_SERVER = "http://127.0.0.1:11901"